# Aeterna — Landing Page

Not started. This is the public marketing/beta-signup site — separate from
the product itself in [`../Desktop App/`](../Desktop%20App/), which
currently has its own (in-app) landing route at `/`.
