# Aeterna — Mobile App

Not started. Per CLAUDE.md's platform decision, the MVP is desktop-only
(Tauri, in [`../Desktop App/`](../Desktop%20App/)); mobile is explicitly
Phase 2, planned as a Capacitor wrapper around the same web UI once the
desktop app's core flows are proven.
